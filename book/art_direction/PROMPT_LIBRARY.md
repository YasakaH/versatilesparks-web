# Prompt Library

## Master Suffix (append to every prompt)

```
Ultra-premium enterprise engineering visualization. Apple WWDC + Stripe + Palantir Foundry + Linear + NVIDIA presentation quality. Dark navy enterprise aesthetic. Glassmorphism. Brushed aluminum. Soft volumetric lighting. Thin cyan illumination. Extreme depth. Ultra-detailed. Photorealistic CGI. Technical realism. Architectural precision. No text. No UI screenshots. No stock illustration. No cartoon. No people. No logos. Consistent visual language across entire book. 8K.
```

## Chapter Prompts

### Automation Mindset — Hero (Type A)

**Title:** Automation Ecosystem

```
A premium enterprise command center visualization. A glowing translucent pipeline flows from top left to bottom right: a golden icon representing Business Goal at the far left, transitioning through a cyan-lit browser node, then through a glass validation chamber, into a database crystal, and finally emerging as a green decision checkmark at the far right. The pipeline is supported by five brushed aluminum pillars labeled only with geometric symbols. Deep navy background. Soft volumetric cyan glow throughout. Isometric perspective. 8K.
```

---

### Chapter 1 — Anatomy (Type F)

**Title:** Chrome Process Anatomy

```
An exploded MacBook-style cutaway of the Chrome browser. The outer shell is a transparent glass browser window floating at the top. Below it, separated by 3cm, sits the browser process as a brushed aluminum controller. Further down, five distinct processor-like modules float in a row: Renderer (labeled with a tab icon), GPU (triangle icon), Network (globe icon), Storage (cylinder icon), Audio (wave icon). Each module is carbon fiber with thin cyan neon edges. Connection lines (cyan, 1px, glowing) run between them. Pure black background. Photorealistic CGI. Isometric exploded view. 8K.
```

**Title:** Launch Timeline

```
A vertical timeline visualization. Six brushed aluminum platform nodes stacked vertically, connected by a thin cyan neon line running down the left edge. Each node is a matte black glass panel with a single geometric icon: a power icon (top), a plug icon, a document icon, a gear icon, a screen icon, and a checkmark icon (bottom). The nodes are lit from above with soft studio lighting. Subtle cyan glow around the active path. Deep navy background fading to pure black at the edges. Straight-on perspective. 8K.
```

---

### Chapter 2 — Hero (Type A)

**Title:** Page Readiness Phases

```
A horizontal 5-stage visualization of a web page loading. Five glass chambers float in sequence from left to right. Stage 1: empty chamber with a single pulsing cyan dot (Loading). Stage 2: wireframe skeleton outline in faint white lines (Skeleton). Stage 3: glowing structure with cyan nodes connecting (Interactive). Stage 4: fully illuminated chamber with data particles flowing (Complete). Stage 5: the chamber pulses with waves radiating outward (Dynamic). Each stage is progressively more illuminated. Dark gradient background. Isometric 30° perspective. 8K.
```

**Title:** Navigation State Machine

```
A circular state machine diagram. Six brushed aluminum nodes arranged in a ring, connected by bidirectional cyan neon paths. Each node is a glass disc with a geometric symbol: a blank square (Blank), a loading spinner (Loading), a document outline (Interactive), a checkmark (Complete), a refresh arrows (Dynamic Update), and an X (Error). One path is highlighted in bright cyan showing the Blank → Loading → Interactive → Complete path. Other paths are dimmed at 30% opacity to show possible transitions. Deep navy background. Straight-on perspective. 8K.
```

---

### Chapter 3 — Hero (Type A)

**Title:** Reliability Layers

```
A three-layer translucent pyramid floating in a dark space. The base layer (largest) is made of brushed aluminum labeled with a shield icon (Fault Tolerance). The middle layer is frosted glass labeled with an eye icon (Observability). The top layer is illuminated cyan glass labeled with a gear icon (Configurability). Each layer is separated by a thin gap with cyan light bleeding through. Soft volumetric lighting from above. Particles floating around the structure. Deep navy background. Straight-on perspective. 8K.
```

**Title:** Recovery Architecture

```
A complex engineering diagram showing a central Recovery Manager as a large hexagonal glass node. Four failure type icons orbit around it: a broken screen (Browser Crash), a clock (Timeout), a key (Session Expired), a warning triangle (Unknown). From each failure icon, a cyan or red path leads to a strategy node: Restart, Retry, Re-login, or Stop. The Stop node is red and connected to an Alert bell. Active paths are cyan, blocked paths are dimmed. Carbon fiber background panel. Isometric perspective. 8K.
```

---

### Chapter 4 — Anatomy (Type F)

**Title:** DOM Anatomy

```
A layered architectural cross-section of a modern web page. The bottom layer is a simple HTML structure (blueprint-style lines). The second layer is jQuery-era DOM with dynamic nodes appearing. The third layer shows React's virtual DOM as a transparent overlay with fiber-like connections to the real DOM beneath. The fourth layer shows Shadow DOM boundaries as translucent frosted glass containers with internal elements visible inside. The top layer shows a canvas element as a solid dark surface with no visible internal structure. Each layer is physically separated and connected by thin vertical cyan lines. Isometric exploded view. Deep navy background. 8K.
```

---

### Chapter 5 — Hero (Type A)

**Title:** Identity Graph

```
A constellation-style network graph. At the center, a glowing cyan node representing Identity. Six satellite nodes orbit at different distances: a key (Password), a document (Session), a hexagon (Token), a folder (Profile), a checkmark (Permissions), and a shield (Browser Fingerprint). Each satellite is connected to the center by a thin neon line — cyan for active, amber for expired, red for blocked. One path (Session → Token) shows a break with red particles scattering. Deep navy background with subtle grid lines. Isometric perspective. 8K.
```

---

### Chapter 6 — Process (Type C)

**Title:** Data Pipeline

```
A horizontal pipeline visualization. Seven glass chambers connected by cyan neon tubes: Capture (a glowing browser window), Parse (a document with parsing lines), Validate (a shield with checkmark), Normalize (a funnel icon), Store (a database cylinder), Serve (a dashboard screen), Archive (a time icon). Each chamber is progressively more illuminated — Capture is dim, Store is bright cyan, Archive is a calm emerald. Particles flow through the tubes from left to right. A small red particle stream diverts at Validate into a side chamber labeled Quarantine. Deep navy background. Straight-on perspective. 8K.
```

---

### Chapter 7 — Hero (Type A)

**Title:** Detection Layers

```
A layered security visualization. Seven horizontal bands stacked vertically, each representing a detection layer. From top to bottom: Network (a glowing globe with connections), Browser (a translucent browser icon), Behavior (a path with waypoints), Timing (a clock with sweep lines), Volume (a bar graph filling), Account (a person silhouette with a shield), History (a timeline with markers). Each band glows from left to right — green at the left (safe), transitioning through amber (monitoring) to red at the right (blocked). The top three bands are green. The bottom four are amber shifting to red. Cyan rim lighting on the edges. Deep navy background. Straight-on perspective. 8K.
```

---

### Chapter 8 — Anatomy (Type F)

**Title:** Starter Kit Exploded

```
A motherboard-style exploded view of the Starter Kit directory structure. The main board is a dark carbon fiber rectangle. Six modules float above it, connected by thin cyan traces: common/ (a chip with multiple pins, largest module), recipes/ (a row of smaller chips), profiles/ (a storage module with partition lines), runs/ (a log with timeline markings), docker/ (a container icon module), and .env (a key-shaped module). Each module is brushed aluminum with glass tops showing internal complexity. The common/ module has sub-labels visible through the glass: browser, retry, logging, config, session, recovery. Deep navy background. Isometric exploded view. 8K.
```

---

### Chapter 9 — Architecture (Type B)

**Title:** CDP Event Flow

```
A vertical architecture visualization. Six layers stacked from bottom to top, connected by glowing neon data streams: Website (bottom, earth-tone icon), Renderer (carbon fiber processor), Chrome Browser (brushed aluminum controller), CDP Protocol (cyan glowing tunnel-like layer), WebSocket (pulsing connection line), nodriver (glass receiver node), Python App (matte panel with data flowing into a queue). Data particles flow upward through each layer as glowing cyan dots. At the nodriver layer, particles enter a glass queue chamber and emerge processed at the Python level. Multiple depth layers with subtle parallax. Deep navy background. Isometric 30° perspective. 8K.
```

---

### Chapter 10 — Hero (Type A)

**Title:** Environment Divergence

```
Four identical workstation setups shown side by side, slowly diverging across the frame. Workstation 1 (far left): pristine, perfectly aligned, labeled with subtle icons for Chrome 128, English, IST, Windows. Workstation 2: slightly shifted — Chrome icon shows version 129, timezone icon changed. Workstation 3: more divergence — font icon different, screen size slightly different. Workstation 4 (far right): completely different — Linux icon, UTC clock, different screen proportions, missing font icons. Cyan connection lines between them start straight on the left and become increasingly distorted toward the right. Each workstation is a brushed aluminum monitor silhouette with a glass screen showing its configuration. Deep navy background. Straight-on perspective. 8K.
```

---

### Chapter 11 — Anatomy (Type F)

**Title:** Interaction Complexity Pyramid

```
A 3D layered pyramid, sliced horizontally into seven translucent levels. Each level is a different material: Clicks (matte polymer, bottom, largest), Keyboard (brushed aluminum), Drag (frosted glass), iframe (glass with internal structure visible), Shadow DOM (carbon fiber with glowing internal nodes), Virtualized Lists (dynamic particles cycling), Canvas (solid dark surface, top, smallest). Each level is connected to the one above by thin cyan pillars. The pyramid floats in a dark space with particles drifting around it. 30° isometric perspective. Deep navy to pure black gradient background. 8K.
```

---

### Chapter 12 — Architecture (Type B)

**Title:** Production System Architecture

```
A massive enterprise architecture visualization. Six interconnected subsystems arranged in a logical flow from left to right: Scheduler (a clock-like node at far left), Worker Pool (three parallel brushed aluminum processing units), Browser Pool (a row of translucent Chrome icons), Validation Pipeline (a glass chamber with green/red sorting), Database Cluster (stacked storage cylinders), and Alerting (a bell with cyan ripple waves at far right). Connection lines (cyan neon, 1px) flow between all subsystems. A glowing path highlights the data flow through the entire system. Operator Dashboard floats above as a translucent glass panel showing health metrics. Deep navy background. Isometric perspective. 8K.
```

---

### Chapter 13 — Hero (Type A)

**Title:** Trust Pyramid

```
A translucent glass pyramid floating in a dark space. Five horizontal layers visible through the glass: Extracted Data (bottom, dim red tint), Validated Data (amber tint), Normalized Data (warm white), Provenance-Attached (cyan glow), Trusted Data (top, bright emerald glow). Each layer is separated by a thin gap with light bleeding through. A pipeline enters from the left into the bottom layer and exits from the top layer to the right as a bright emerald beam. Data particles flow through each layer, with some red particles being filtered out at the Validation level. Deep navy background. Straight-on perspective. 8K.
```

---

### Chapter 14 — Architecture (Type B)

**Title:** Complete Automation Platform

```
A massive full-system architecture visualization spanning the entire frame. Five interconnected subsystems arranged in a logical grid: Price Monitor (product database + extraction pipeline), SaaS Dashboard (session management + report generator), CRM Lead System (form processing + dedup engine), Supplier Pipeline (multi-worker extraction with profile isolation), Operations Platform (scheduler + job registry + worker pool + health dashboard). Connection lines flow between systems showing data handoffs. A translucent Operator Dashboard overlay shows health metrics: 5 jobs running, 0 failures, 124K records today. Deep navy background. 30° isometric perspective looking down across the entire platform. 8K.
```

---

### Operations Guide — Hero (Type A)

**Title:** NOC Dashboard

```
A Network Operations Center visualization. Multiple translucent dashboard panels float in a dark room: a timeline panel showing the past 24 hours with green/amber/red bars, a system health panel showing 5 services with status indicators, a queue depth panel showing a bar graph approaching a threshold, an active incidents panel listing 3 items with severity indicators. The panels are frosted glass with thin cyan borders, floating in a three-dimensional space with depth separation between foreground (urgent alerts), midground (system health), and background (timeline/history). A single bright amber alert glows in the top-right panel. Dark atmosphere with volumetric lighting from below. Straight-on perspective. 8K.
```
