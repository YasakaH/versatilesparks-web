# Question: Why is data extraction missing content?
Mode: troubleshoot

=== Answer A ===
--- Avoiding Bot Detection ---
Techniques to avoid browser fingerprinting detection: spoof WebDriver flag, override navigator properties, use realistic user agents, mimic human behavior. Scrapers get blocked because sites detect automated behavior patterns: fixed intervals, no mouse movement, headless browser signatures.

--- Blocking Detection and Rate Limiting ---
Sites detect scrapers through behavioral signals: request frequency, IP reputation, missing headers, no JavaScript execution, headless browser signatures, and abnormal mouse/keyboard patterns. Rate limiting blocks by counting requests per time window. Captchas add a proof-of-human step. Bans escalate from temporary (429 status) to permanent (403/blocked IP).

--- Isolated Browser Profiles ---
Browser profiles provide isolated environments with persistent storage, cookies, and local state.

=== Answer B ===
# Troubleshooting

**Problem:** Why is data extraction missing content?

## Likely Causes
- **Page Data Extraction** (probability: high)
  - Evidence: Extract page data incrementally: wait for DOM ready -> extract visible content -> scroll -> extract 
  - Diagnostic: Check Page Data Extraction configuration
  - Fix: Review Page Data Extraction documentation
- **Production Download Pipeline** (probability: high)
  - Evidence: A download pipeline manages browser-based data extraction at scale: queue URLs, launch browser sessi
  - Diagnostic: Check Production Download Pipeline configuration
  - Fix: Review Production Download Pipeline documentation
- **Blocking Detection and Rate Limiting** (probability: medium)
  - Evidence: Sites detect scrapers through behavioral signals: request frequency, IP reputation, missing headers,
  - Diagnostic: Check Blocking Detection and Rate Limiting configuration
  - Fix: Review Blocking Detection and Rate Limiting documentation

## External Factors
- Browser version differences
- Anti-detection mechanisms
