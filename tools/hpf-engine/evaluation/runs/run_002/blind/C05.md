# Question: Exponential backoff vs fixed backoff
Mode: compare

=== Answer A ===
--- Avoiding Bot Detection ---
Techniques to avoid browser fingerprinting detection: spoof WebDriver flag, override navigator properties, use realistic user agents, mimic human behavior. Scrapers get blocked because sites detect automated behavior patterns: fixed intervals, no mouse movement, headless browser signatures.

--- Retry with Backoff for Browser Operations ---
Retry with exponential backoff for transient browser failures (navigation timeout, element not found, staleness).

=== Answer B ===
# Retry with Backoff for Browser Operations

**Definition:** Retry with exponential backoff for transient browser failures (navigation timeout, element not found, staleness).

## Core Mechanics
- Retry with exponential backoff for transient browser failures (navigation timeout, element not found, staleness)

## Examples
- Browser automation faces inherent flakiness
- Retry with jittered backoff improves reliability 10x
- Implement with circuit breaker to avoid hammering a dead browser

## Limitations
- No limitations documented
