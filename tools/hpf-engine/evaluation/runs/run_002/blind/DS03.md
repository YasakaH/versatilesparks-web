# Question: Design a retry mechanism for CDP operations
Mode: design

=== Answer A ===
# Design

**Problem:** Design a retry mechanism for CDP operations

## Approach Options
- **Retry with Backoff for Browser Operations:** Retry with exponential backoff for transient browser failures (navigation timeout, element not found, staleness).
- **Chrome DevTools Protocol:** CDP (Chrome DevTools Protocol) is a protocol for instrumenting Chromium-based browsers. It exposes JSON-RPC over WebSocket for controlling browser beh
- **Nodriver - CDP without WebDriver:** Nodriver is a library that connects to the browser directly via CDP over WebSocket, bypassing WebDriver entirely.

**Recommended:** Retry with Backoff for Browser Operations

## Common Pitfalls
- Ignoring Chrome DevTools Protocol can cause issues
- Ignoring Nodriver - CDP without WebDriver can cause issues

## Best Practices
- Start simple
- Add complexity only when needed

=== Answer B ===
--- Avoiding Bot Detection ---
Techniques to avoid browser fingerprinting detection: spoof WebDriver flag, override navigator properties, use realistic user agents, mimic human behavior. Scrapers get blocked because sites detect automated behavior patterns: fixed intervals, no mouse movement, headless browser signatures.

--- Blocking Detection and Rate Limiting ---
Sites detect scrapers through behavioral signals: request frequency, IP reputation, missing headers, no JavaScript execution, headless browser signatures, and abnormal mouse/keyboard patterns. Rate limiting blocks by counting requests per time window. Captchas add a proof-of-human step. Bans escalate from temporary (429 status) to permanent (403/blocked IP).

--- Isolated Browser Profiles ---
Browser profiles provide isolated environments with persistent storage, cookies, and local state.
